services:
  - type: web
    name: textdistance-rs
    runtime: rust
    plan: free

    buildCommand: cargo build --release --bin web

    startCommand: ./target/release/web

    envVars:
      - key: RUST_LOG
        value: info